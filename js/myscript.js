$(document).ready(function () { // run only after the dom is fully loaded
    $('h2').addClass('underline');
    $('ul').addClass('border');
    $('').removeClass('');
    $('').css('');
    $('').html();
    $('').html(''); // replace html content
    $('').remove(); // element
    $('').empty(); // element
    $('').text(''); // replace text in element
    $('').append(); // add to to the end of existing element$('')
    $('div > p').html();
    $('div p').html();
});